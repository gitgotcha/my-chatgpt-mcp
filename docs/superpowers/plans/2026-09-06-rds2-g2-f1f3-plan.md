# RDS V2 G2 补充修订方案（F1–F3）· 第二阶段修订稿

方案日期：2026-09-06。审核基准 HEAD：`43ec6f2`。

**v3**：已并入开工前的 5 项修正 —— 预算耗尽的三类分流（§3.2.1）、分阶段预算闸门（§5.3）、
状态/归档限额分列（§2.5）、staging 读取显式绑定 scope（§2.2、§2.3）、范围契约收紧（§4.2、§4.3）。
已获准按 **F2 → F1 → F3** 开始本地修复；本文档为实施依据，不再单独等一轮纯文档审核。

仍然：未进入 T09/T10、未 push、未部署、未操作远程资源、未清理主仓库遗留。
**本许可是修复方案开工许可，不是 G2 验收通过。**

---

## 0. 本轮修订摘要

### 0.1 四项开放项已裁定（本稿据此定稿）

| 开放项 | 裁定 | 落实位置 |
|---|---|---|
| 告警出口 | G2 本轮做**持久化的 `needs_attention` + 稳定原因 + 状态转换成功后的脱敏结构化日志**；外部通知留到可观测性工作包，发布前闭环。**不得把日志称为「通知已送达」** | §3.6 |
| D 类计数 | **不增加 `failure_count`**，直接停车并记录确定性原因；已有失败计数**不抹除** | §3.2、§3.3 |
| staging 点查 | 接受**最多 4 次**，不强求合并；但**必须由引擎强制限制**，总调用仍满足**投影配额 24**，不能只证明「低于 40」 | §2.3、§5 |
| 超限错误码 | 保留独立 **`build_state_too_large`**，归 D 类 | §2.5、§3.2 |

### 0.2 六处修正对照

| # | 修正要求 | 本稿落点 |
|---|---|---|
| 1 | F1 明确异步接口与调用约束 | §2.2 —— 改为「reducer 声明读取计划 → 引擎校验去重执行 ≤4 次 → 纯 `buildPage`」；页内 Map 规则见 §2.4 |
| 2 | 固定字段数 ≠ 固定字节；不得新增更小限制 | §2.5、§2.6 —— 取消 4KiB 预设，改为量测上界作**观测断言**而非拒绝阈值；修正 35/200/1000 表述 |
| 3 | 不能把所有无白名单 code 的异常都视为临时故障 | §3.1、§3.2 —— 先双绑定验证异常形状，再建统一转换层；分类覆盖读回 staging 与序列化阶段 |
| 4 | 补齐零行收尾与成功清零 | §3.3、§3.4 —— 调用方先查 `rowsWritten`；`completeTask` 成功事务内清零；预算预留落实为检查与完整账目（§5） |
| 5 | 重新定义范围，不要求实际序号首尾相接 | §4.2 —— 区分**扫描窗口**与**实际消费范围**；消费结果必须是读取数组的非空前缀 |
| 6 | 补完整形状校验，消除矩阵矛盾 | §4.3、§4.5 —— 数组/逐项/安全整数/空构建固定编码/游标分轨；重复页号拒绝、乱序输入**通过** |

另按裁定：**不再为文档自身的提交反复修改历史提交表**（§1 末）。

---

## 1. 只读核对（接手时状态，未变）

| 项 | 实测 |
|---|---|
| 分支 | `feat/rds2-v2` |
| HEAD | `43ec6f2`，与审核基准一致 |
| 已跟踪文件改动 | 无 |
| 未跟踪 | 补充审核文档 + 本方案文档（均保留） |
| `4f69982` | 完好（`builds.js:243–259` 游标双上界仍在），未回退 |
| 主仓库 Rev4 遗留 | 未触碰 |

本轮业务子请求消耗为 0（未发起任何 D1 执行、队列发送或 HTTP 调用）。本地文件检视是工程动作，与 Worker 子请求预算不是同一口径，二者不作比较。

---

## 2. G2-F1 [P1] 有界 staging 状态（修订）

### 2.1 根因（不变）

- `algorithm.js:214–330` `buildPage` 把 `topics{}`、`problems{}`、`latestByTopic{}` 三份全历史字典放进 `continuation`。
- `builds.js:304–311` 整份回写 `savedContinuation`。
- `migrations/0006_rds2_v2_tables.sql:126` 约束 `continuation_json … length(...) <= 65536`。
- 字节随「不同专题数 + 不同题目数」单调增长，第 5 页触发 CHECK → 落入 §3 的 defer 死循环。

关键观察（不变）：每页已把 topic/problem 累计值以键控行 upsert 进 staging generation（`builds.js:321–336`），reducer 只是读不回来。

### 2.2 接口：声明式读取计划 + 引擎执行 + 纯函数折叠（**修订**）

原稿给出异步 `readStaged` 却未 `await reducer.buildPage(...)`，不可直接实现。改为三段式，**`buildPage` 保持纯同步函数**：

```js
// ① reducer 声明本页需要读什么（纯函数，无 IO、无 await）
const plan = reducer.planPageReads({ events });
//   → { reads: [ { rowKind: "topic",   rowKeys: [...] },
//                { rowKind: "problem", rowKeys: [...] } ] }
//     rowKeys 由 reducer 去重、顺序稳定；不含 IO

// ② 引擎校验并执行（有界、可预算、可被拒绝）
const staged = await readStagedWithinBudget({
  io, scope, stagingGeneration, plan, maxCalls: 4
});
//   → { topic: Map, problem: Map }
//
//   scope 与 stagingGeneration 一律由引擎从权威构建行取得，**不由 reducer 传入**：
//   每条查询都必须同时绑定 userId、namespace、projectionName、generation 四段。
//   引擎在**外发任何一次查询之前**完成计划校验、同类合并、去重与总成本测算 ——
//   不允许「先发 4 次才发现计划需要第 5 次」。若去重分块后仍超过 maxCalls，
//   直接抛 `build_read_limit_exceeded`（D 类），一次查询都不发。

// ③ 纯折叠
const pageResult = reducer.buildPage({ scope, events, head, continuation, staged });
```

`readStagedWithinBudget` 的**引擎强制约束**（不是 reducer 自律）：

1. `rowKind` 必须在白名单 `{ topic, problem }` 内，否则确定性拒绝；
2. 单个 `rowKind` 的 key 数 ≤ 本页事件数（≤ `pageSize` ≤ 50）；
3. 分块：每块 ≤ 32 个 key，SQL 必须形如
   `… WHERE user_id = ? AND namespace = ? AND projection_name = ? AND generation = ?
      AND row_kind = ? AND row_key IN (?, ?, …)`，
   **四段 scope + generation 全绑定**，走既有唯一键点查，非扫描；
3b. **发前测算**：先按去重后的键数算出块数与总调用数，超出 `maxCalls` 就在**发第一次之前**拒绝；
3c. **同类合并与去重**：同一 `rowKind` 的 key 先去重再分块，重复键不产生额外查询；
4. **总调用次数 ≤ 4**，第 5 次直接抛 `build_read_limit_exceeded`（D 类），不静默退化为「少读几个键」；
5. 每次调用都经 `io.db` 包装计数，**失败的读取同样计数**（`budget.js` 先 consume 后发出）；
6. 引擎在业务阶段开始前检查预算预留（§5），预留不足时不得进入读取阶段。

由此 reducer **无法**通过反复调用读取接口消耗预算：它只声明一次计划，执行权与次数上限都在引擎。

### 2.3 调用次数上界与配额（裁定：接受 4 次，但引擎强制、总量满足 24）

- 本页不同 key 数受页长限制：`topic` ≤ 50、`problem` ≤ 50 → 每 kind 最多 2 块（块长 32）→ **合计 ≤ 4 次**。
- 这 4 次计入投影配额 24 之内，完整账目见 §5。**验收口径是「总调用 ≤ 24」而非「低于 40」**。
- 不强求合并为单次 `row_kind IN (…)` 查询（裁定接受 4 次）；若后续实测希望降为 2 次，作为优化项，不阻塞本轮。

### 2.4 页内重复键：必须读页内更新后的值（**修订新增**）

同一页内重复出现同一专题或题目时，必须更新**页内临时 Map**，后续事件读取的是更新后的值，不能每次都从预读值重新计算：

```js
const working = new Map(staged.topic);        // 页内可变副本
for (const event of events) {
  const current = working.get(topic) ?? null; // ← 读页内当前值，不是预读快照
  const next = updateTopic(current, { outcome: learning.outcome });
  working.set(topic, next);                   // 后续事件看到更新后的值
  rowChanges.push({ rowKind: "topic", rowKey: topic, value: next });
}
```

`problem` 同理。`latestByTopic` 不再单独保存：topic 行 value 已含 `lastObservedAt`/`lastEventId`/`lastOutcome`，用 `latestWins` 对页内当前值与本次候选比较即可重算，等价且省一份字典。

回归面：跨页回访已有键、页内重复键、页内重复键且后一次是更晚事件（胜者应更新）都必须累计正确。

### 2.5 字节上界：不预设数值，先消除字典再量测（**修订**）

原稿「上限 ≤ 4KiB」是拍脑袋，撤回。原因：`topic`、`username` 等是用户可控的**变长**字符串，字段数量固定不等于字节固定。

新口径：

1. **消除全历史字典**（`topics{}`/`problems{}`/`latestByTopic{}`），continuation 只保留定长标量 + 单个 head + 固定 4 个计数；
2. **不重复保存 summary 已有字段**：`counts`、`latest`、`headIdentity`、`lastReceivedEventId` 只存一份，不在 continuation 顶层与 `summary` 内各存一遍；
3. **量测得出上界 C 并写死为观测断言**（`continuationBytes ≤ C`），C 由实测记录，**C 只作断言，不作拒绝阈值**；
4. **事务前校验**各受限单元，**限额按对象分别适用**（原稿一律写 64KiB 会误拒绝合法归档，已修正）：

   | 对象 | 保留限额 | 超限处理 |
   |---|---:|---|
   | `continuation`、`summary`、单行 `value_json` | **64 KiB** | `build_state_too_large`（D 类，停车，不 defer） |
   | `packageJson`、`activationJson`（归档对象） | **256 KiB** | 沿用既有归档超限码，**不并入** `build_state_too_large` |
   | 行变更集合 | 既有限额 | 沿用 `changes_too_large` |

   错误码保持分列，不全部合并：`build_state_too_large` 只表示**构建状态**超限；
   既有行变更、归档超限码继续表达各自语义。

5. **不得新增更小限制**：不引入任何低于上表既有值的自定义拒绝阈值，避免原本能正常处理的合法输入被无依据拒绝。量测出的 C 仅用于暴露回归，不用于拒绝。

### 2.6 量测场景表述修正（**修订**）

原稿「同一 35 条事件构造 200/1000 个专题」表述错误（35 条事件造不出 1000 个专题）。修正为：

- 分别构造 **35 条、200 条、1000 条或更多**事件，各自带不同专题与不同题目；
- 每组断言：构建可完成、重放等于在线结果、`readStaged` 调用次数 ≤ 4、continuation 字节 ≤ C；
- **不得只复用一个专题和一道题**，否则无法暴露累计状态增长。

---

## 3. G2-F2 [P1] 错误分类与真实故障生命周期（修订）

### 3.1 前置动作：先用双绑定验证异常形状（**修订新增**）

裁定指出：数据库守卫用 `RAISE(ABORT, 'stale_task_write')` 等错误文本，真实 D1 **未必提供同名 `error.code`**。

实测确认：当前 `src/` 中**完全没有** `stale_task_write` / `cursor_regression` / `build_requires_building` 的任何识别点 —— 这些受控错误目前一律落进通用 catch 被 defer。

因此实施第一步不是写分类，而是**探测**。**已于 2026-09-06 完成探测，实测结果如下**（探针：
`.rds2-probe-f2-shape.mjs`，未提交，待断言进入正式测试后删除）。

#### 3.1.1 实测异常形状

| 绑定 | `error.code` | `error.message` | `error.cause` | 守卫标记所在 |
|---|---|---|---|---|
| SQLite 模拟器 | `"ERR_SQLITE_ERROR"` | **恰为守卫文本**，如 `cursor_regression` | 无 | `error.message` |
| Miniflare/workerd D1 | **`null`** | `D1_ERROR: cursor_regression: SQLITE_CONSTRAINT (extended: SQLITE_CONSTRAINT_TRIGGER)` | 有，`cause.message` = `cursor_regression: SQLITE_CONSTRAINT (…)`，`cause.code` 也是 `null` | `error.message` 与 `error.cause.message` |

对照组（普通 SQL 错误，非守卫）：SQLite `no such table: rds2_no_such_table`；
D1 `D1_ERROR: no such table: rds2_no_such_table: SQLITE_ERROR`；两者 `code` 均非守卫标记。

**结论（直接决定实现方式）**：

1. **两种绑定都不提供等于守卫名的 `error.code`** —— 审核的预判成立
   （SQLite 给 `ERR_SQLITE_ERROR`，D1 给 `null`）。
   分类**必须**基于消息/原因文本中的**守卫标记**，不能依赖 `error.code`。
2. **在条件隔离的前提下，守卫 → 标记是确定的 1:1 映射，两种绑定完全一致**（§3.1.2）。
   转换层因此**必须保留具体标记**，再结合操作上下文定类别，**不得把多个守卫合并成一类**。
3. 辅助判据（仅用于加速，不作唯一依据）：SQLite `errcode` `1811` = 触发器中止、
   `275` = CHECK、`1` = 一般 SQL 错误；D1 用 `extended: SQLITE_CONSTRAINT_TRIGGER` /
   `SQLITE_CONSTRAINT_CHECK` / `SQLITE_ERROR`。

> **已撤回的错误结论（第一版探针）**：初版用 `no-such-task` 触发 `stale_task_write`，
> 该构造**同时**违反任务守卫与投影关联守卫，得到的是 `stale_projection_revision`。
> 我当时据此断言「触发器执行顺序不确定、应把整个守卫标记集归为同一类」—— **这是错的**。
> 一个用例同时踩中多个失败条件，只说明存在多个失败条件，不能据此合并任何类别。
> 隔离条件后重测（§3.1.2），每个用例在两种绑定上都只产生**唯一且确定**的标记。

#### 3.1.2 隔离条件下的 1:1 标记映射（实测）

每个用例只违反**一个**条件、且使用独立的 `user_id`，schema 每绑定只 apply 一次。
两种绑定对每个用例都给出**同一个标记**，无歧义、无顺序依赖：

| 用例（只违反该条件） | 标记 | SQLite 载体 | D1 载体 |
|---|---|---|---|
| 租约 owner 不匹配（其余全有效） | `stale_task_write` | `message`，`errcode 1811` | `message` / `cause.message` |
| 租约 epoch 不匹配 | `stale_task_write` | 同上 | 同上 |
| 租约已过期 | `stale_task_write` | 同上 | 同上 |
| `expected_revision` 与 head 不符（租约全有效） | `stale_projection_revision` | 同上 | 同上 |
| 缺 `expected_revision`（projection 任务） | `guard_requires_expected_revision` | 同上 | 同上 |
| `last_event_seq` 倒退 | `cursor_regression` | 同上 | 同上 |
| 未置 `building=1` 即插入 build | `build_requires_building_flag` | 同上 | 同上 |
| 置 `building=1` 后插入 build | **成功**（证明前一行确实只违反一个条件） | — | — |
| continuation 合法大小更新 | **成功**（证明目标行存在） | — | — |
| continuation 超 64 KiB | `CHECK constraint failed: continuation_json IS NULL OR (json_valid(…) AND length(…) <= 65536)` | `errcode 275` | 同文本 + `extended: SQLITE_CONSTRAINT_CHECK` |
| 普通 SQL 错误（对照） | 无守卫标记，`no such table: …` | `errcode 1` | 同文本 + `SQLITE_ERROR` |

**continuation 64 KiB CHECK 已补齐有效样本**（第一版因目标行不存在而无效）：
先置 `building=1` → 插入 build 成功 → 合法大小更新成功 → 再更新超限值才失败。
标记文本含列名与完整 CHECK 表达式，因此 CHECK 类按「出现 `CHECK constraint failed`
且含受限列名」识别，与触发器标记区分开。

#### 3.1.3 标记 → 类别（按裁定，不做合并）

| 标记 | 处理原则 | 类 |
|---|---|---|
| `stale_task_write`、`stale_projection_revision` | 按**租约/CAS 冲突**处理，不计真实故障；但**不得未经核对就返回成功** —— 必须回到权威状态判定（如 `verifyBuildActivated`） | B |
| `guard_requires_expected_revision` | 缺少**必填契约**，不是正常竞态，归确定性错误 | D |
| `cursor_regression` | **不允许一律暂缓**；正常流程中出现即需诊断的不变量违规 | D |
| `build_requires_building_flag` | **不能无条件归 B** —— 按构建启动 CAS 与权威状态判断：CAS 失败属竞态，状态本身不成立属实现/契约问题 | B 或 D（依权威状态） |
| JSON / 大小 CHECK | 确定性约束错误，**不走无限重试** | D |
| 无标记的其他存储异常 | 有限重试（阈值 5 + 退避档位） | C（默认兜底） |

#### 3.1.4 测试分两层（按裁定）

| 层 | 用例形态 | 断言 |
|---|---|---|
| **单层（单一失败条件）** | 只违反一个条件，且用独立的 scope | **断言精确标记**（如 `lease_epoch_mismatch` 必须是 `stale_task_write`，不能接受其他标记）；同时断言该条件成立前的前置操作成功（如「置 `building=1` 后插入 build 成功」） |
| **多层（多条件同时失败）** | 故意同时违反多个条件 | **允许返回已知标记之一**，但必须断言：① 整批回滚（无部分写入）；② **不返回错误成功回执**；③ 类别判定不依赖拿到的具体是哪一个标记 |

**两层都不得依赖触发器执行顺序**；测试只断言「类别 + 回滚 + 无假成功」，不断言具体触发器名。

「内部精确识别受控错误标记」与「禁止输出敏感错误文本」是两件事，二者同时满足：识别要准，输出要净。

### 3.2 分类表（修订）

| 类 | 判据 | 收尾 | `failure_count` | 退避 | 结果 |
|---|---|---|---|---|---|
| **A 正常等待** | 预算保留、等前序事件、正常续页、构建被上游中止后重排 | `deferTask(availableAt)` | 不增加 | 按等待原因 | `retry / deferred_*` |
| **B 已识别竞态** | 转换层识别为租约/CAS 竞争；或收尾 `rowsWritten === 0`；或 `verifyBuildActivated` 为真 | `noop` / `completed` | **不增加** | — | `lease_lost` / `build_activated` |
| **C 临时存储故障** | 转换层未识别、且不属于 D 白名单的存储异常（**默认兜底**） | `failTask(code)` | **+1** | 30/60/120/240/480 s | `retry` → 第 5 次 `needs_attention` |
| **D 确定性契约/大小错误** | code ∈ 白名单 | `parkNeedsAttention(稳定 code)` | **不增加**（裁定：直接停车、记录确定性原因；已有计数不抹除） | — | `needs_attention` |
| **E 收尾自身不可用** | `failTask`/`parkNeedsAttention` 自身抛错；**或预算已耗尽且连收尾预留都不足** | 保留租约（可用时），返回明确失败结果 `budget_exhausted_no_closeout` | 不增加（不得谎报已持久化） | — | 交恢复机制，**保留恢复路径** |

**C 是默认兜底**：凡转换层未识别、又不在 D 白名单内的，一律按 C 处理并**有限重试**（受阈值 5 与退避档位约束），不会无限暂缓。

**D 类白名单**（闭合集合）：
`changes_too_large`、`commit_batch_too_large`、**`build_state_too_large`**（§2.5，构建状态超限专用）、
`build_read_limit_exceeded`（§2.2）、`build_continuation_invalid`、`build_no_progress`、
`invalid_page_size`、`projection_head_missing`、`implementation_budget_violation`（§3.2.1）、
以及由转换层（§3.1.3）归一进来的：`cursor_regression`、`guard_requires_expected_revision`、
**CHECK 类**（`storage_check_violation`，含 `continuation_json` / `summary_json` 超限）。

**`build_requires_building_flag` 不在此白名单** —— 它是**条件性**的：按构建启动 CAS 与权威
状态判定，CAS 竞争归 B，状态本身不成立归 D（§3.1.3）。

**`budget_exhausted` 不在 D 类白名单内** —— 见 §3.2.1。

#### 3.2.1 预算耗尽的三类分流（**修正**）

原稿把 `budget_exhausted` 一律归 D 类「确定性、重试无意义」是**错的**：预算是**每次调用独立**的，
下一次调用完全可能正常处理。按情形分流：

| 情形 | 判定点 | 处理 | 类 | `failure_count` |
|---|---|---|---|---|
| **主动发现余量不足** | 阶段闸门预检（§5.3）发现 `剩余 < 下一阶段最坏成本 + 4`，尚未消耗 | `deferTask`，code `budget_reserve_insufficient`，**正常暂缓** | **A** | **不计真实失败** |
| **运行中耗尽，收尾预留仍足** | 业务阶段中途耗尽，但预留 4 次未动 | 按原路径收尾：竞态 → B；存储异常 → C（`failTask`） | B / C | 按各类规则 |
| **运行中耗尽，收尾预留也不足** | 连收尾都发不出去 | 明确失败 `budget_exhausted_no_closeout`，**保留租约与恢复路径，不谎报已保存状态** | **E** | 不计 |
| **代码路径必然超出固定配额** | 实测/断言发现某路径的**下限**就超过配额（非数据所致） | 独立诊断 `implementation_budget_violation`，停车并标记为实现问题 | **D** | 不计 |

要点：**不能把所有预算耗尽都归为永久错误**。只有最后一类才是实现缺陷；前三类都是运行时状态，
必须保留下一次调用恢复的可能。

**分类覆盖范围（修订）**：不只是最终 `io.db.batch`。以下阶段同样接入转换层与分类：
- `readStaged` 的 staging 读回；
- continuation / summary / 行值的序列化与字节测算；
- 归档对象 JSON 的构造与哈希；
- 收尾写（`failTask` / `park` / `completeTask`）自身。

### 3.3 零行收尾：调用方必须先查写入结果（**修订新增**）

实测：`failTask` 即使 `rowsWritten === 0`，其返回值仍可能给出 `state: "pending"`（判定式含 `rowsWritten && …`）。因此：

- 调用方**必须先检查 `rowsWritten`**，`rowsWritten === 0` 时按 **B 类**（租约被接管）处理，**不得声称已保存退避或失败计数**；
- 返回结构需明确区分「已持久化退避」与「未写入」两种结果，供调用方与测试断言。

### 3.4 成功清零：在成功事务内处理，不靠人工重放（**修订新增**）

实测：`completeTask` 只写 `state/lease`，**没有** `failure_count = 0`。`requeueNeedsAttention` 的清零属于**人工重放**路径，不能替代成功清零。

规则：

- 任务成功收尾（构建页完成、构建激活、增量投影完成）时，在**同一个成功事务内**把 `failure_count` 归零；
- **不得把「scope 内其他任务成功」当作本任务成功**：清零只针对本任务自身；
- 人工重放（`requeueNeedsAttention`）的清零语义保持不变。

### 3.5 与 Rev6 addendum §8 的对齐

| §8 契约 | 现有 | 本方案 |
|---|---|---|
| 真实故障计数 | `failTask` 已有，投影路径未接 | 接上（C 类） |
| 按规则退避 | `FAILURE_BACKOFF_SECONDS` 已有 | 同上 |
| 连续 5 次 → `needs_attention` | `FAILURE_COUNT_THRESHOLD = 5` 已有 | 同上 |
| 连续 5 次「告警」 | **无任何通知出口** | 按裁定：本轮做持久化 `needs_attention` + 稳定原因 + 脱敏结构化日志（见 §3.6）；外部通知留可观测性工作包，发布前闭环 |

### 3.6 告警口径（裁定落实）

- 状态转换**成功之后**才写日志；转换失败不写成功日志。
- 日志为脱敏结构化字段：`taskId`（可哈希截断）、`taskType`、`outcome`、`failureCount`、`threshold`、`stableCode`、`at`；**不含**原始 `message`、SQL、绑定参数、用户内容。
- 文档与日志文案中**不得**将该日志表述为「通知已送达 / alert sent」；外部通知不在本轮。

---

## 4. G2-F3 [P1] 重放完整性校验与测试矩阵（修订）

### 4.1 现状缺口（不变）

`archiver.js:159` scope 条件校验；`:196` 只比 `baseRevision` 未要求 `revision = baseRevision + 1`；`:205` 使 `pages = 0` 配非空范围通过；`:160` 用 `JSON.stringify(scope)` 比较（依赖键序）；未逐页校验 scope/generation/buildId/范围；`builds.js:281` 包 `lastEventSeq` 取「本次读取」而非「实际消费」的最后一条。

### 4.2 范围契约：扫描窗口 vs 实际消费范围（**修订重写**）

原稿要求「实际序号首尾相接」是错的。反例：A 用户两页分别消费 `[1,3]`、`[5,7]`，中间序号属于 B —— 两页实际范围不连续，但完全合法。

区分两个概念：

| 概念 | 定义 | 连续性要求 |
|---|---|---|
| **扫描窗口** | 本页**起始游标** → **消费后游标**，**半开区间 `[scanFirstCursor, scanCursorAfter)`** | 相邻页窗口**首尾衔接**：第 n 页 `scanFirstCursor` = 第 n−1 页 `scanCursorAfter`。窗口是游标区间，允许内含他用户序号 |
| **实际消费范围** | 本页真正消费的 `{ consumedFirstEventSeq, consumedLastEventSeq, consumedCount }` | **不要求**与他页数值连续；只要求它对应本页读取数组的**非空前缀** |

约束：

1. 消费结果必须是读取数组的**非空前缀**：`consumedCount` 为**安全整数**且 `1 ≤ consumedCount ≤ events.length`；
   `events.slice(0, consumedCount)` 的 `eventSeq` 严格递增。
   **第一条消费事件、最后一条消费事件、消费后游标三者全部由引擎从前缀派生**，不由 reducer 自报：
   `consumedFirstEventSeq = events[0].eventSeq`、
   `consumedLastEventSeq = events[consumedCount - 1].eventSeq`、
   `scanCursorAfter = consumedLastEventSeq + 1`；
2. 因此恒有 **`consumedLastEventSeq === scanCursorAfter − 1`**（相等，不是 ≤）；
3. **保留 `4f69982` 的双上界保护**：`scanCursorAfter ≤ lastFetchedSeq + 1` 且 `≤ targetEventSeq + 1`，不满足即 `build_no_progress`（D 类）；
4. **不得**仅凭 `nextEventSeq − 1` 就断言它一定是一条实际消费的事件（合法空页/零消费时不成立）；需要「实际消费的最后一条」时，取 `events[consumedCount − 1].eventSeq`，并同时校验 `consumedCount ≥ 1`；
5. **manifest 与页包各自明确使用哪一种范围**：
   - **页包** `firstEventSeq` / `lastEventSeq` = **实际消费范围**，并附 `consumedCount`；
   - **manifest** `firstEventSeq` = 整个构建的首个实际消费事件（或 `null`），`lastEventSeq` = 冻结目标 `targetEventSeq`；manifest **不要求**页间数值连续。

覆盖判据（替代原「首尾相接」）：

```
page[1].scanFirstCursor === manifest.firstEventSeq   （或 manifest.firstEventSeq 为 null 时为空构建）
page[n].scanCursorAfter === page[n+1].scanFirstCursor   对所有相邻页成立
page[last].scanCursorAfter === targetEventSeq + 1
每页 consumedCount 为安全整数且 1 ≤ consumedCount ≤ events.length
每页 consumedLastEventSeq === scanCursorAfter − 1（相等）
page[n].scanCursorAfter === page[n+1].scanFirstCursor（窗口衔接，依半开区间）
```

**页包必须实际保存两个扫描游标**（`scanFirstCursor`、`scanCursorAfter`），
否则离线重放无从验证窗口衔接 —— 仅保存实际消费范围不足以证明「中间没有整段被跳过」。

他用户造成的序号空洞由「扫描窗口内含未消费序号」天然容纳，**不要求** `consumedLastEventSeq(n) + 1 === consumedFirstEventSeq(n+1)`。

**两处明确例外**：

- **空构建的 `firstEventSeq = null` 是安全整数校验的明确例外**（§4.3 S4），校验器不得因它非整数而拒绝；
- **`events.length` 可为 0**（合法空页），此时不适用 `consumedCount ≥ 1`。

### 4.3 完整形状校验（**修订新增**）

| # | 规则 | 失败 code |
|---|---|---|
| S1 | `changes`（增量 delta）与 `rowChanges`（页包）**必须是数组**；逐项校验 `{ rowKind, rowKey, memberKey?, sortKey?, value }`，`rowKind`/`rowKey` 为非空字符串。**可选字段的 `null` 与缺省规则必须与现有写入契约一致** —— 现有 upsert 对 `member_key`/`sort_key` 写 `?? null`，故校验器须同时接受 `null` 与缺失两种形态，**不得拒绝本系统自己生成的对象**（新校验器上线前先用现有写入产物做正样本自检） | `replay_artifact_invalid` |
| S2 | 所有数字字段（`revision`/`baseRevision`/`eventSeq`/`pages`/`generation`/`consumedCount`/`firstEventSeq`/`lastEventSeq`）用 **`Number.isSafeInteger`** 校验，拒绝浮点、NaN、Infinity、超出安全整数范围 | `replay_artifact_invalid` |
| S3 | `pages` ≥ 0；`generation` ≥ 1；`buildId` 为非空字符串；scope 三字段（userId/namespace/projectionName）均为非空字符串 | `replay_manifest_invalid` / `replay_scope_missing` |
| S4 | 空构建**固定一种编码**：`firstEventSeq = null`、`lastEventSeq = 0`、`pages = 0`；不接受「`lastEventSeq` 为 null 但 `pages = 0`」等变体。**`firstEventSeq = null` 是 S2 安全整数校验的明确例外**（见 §4.2 末），不得因此拒绝 | `replay_range_inconsistent` |
| S5 | 游标规则**分轨**：普通增量 delta 的 `eventSeq` = 该事件自身序号；构建激活 delta 的 `eventSeq` = `targetEventSeq`（冻结目标）。两者都要求 `revision = baseRevision + 1` | `replay_revision_not_chained` |

叠加 §4.2 的范围规则与原有的 R1–R3（scope 逐字段比较、`storageVersion === 2`、kind 匹配）、R10（哈希保留、失败即 throw、不返回部分结果）。

### 4.4 规则汇总（含未修订项）

| # | 规则 | 失败 code |
|---|---|---|
| R1 | 每个 delta 与页包**必须**带完整 scope，三字段非空 | `replay_scope_missing` |
| R2 | scope **逐字段**比较，不比 JSON 字符串，不依赖键序 | `replay_scope_mismatch` |
| R3 | `storageVersion === 2`，`kind` 与 `objectType` 匹配 | `replay_artifact_invalid` |
| R4 | `revision === baseRevision + 1`，且均为安全整数 | `replay_revision_not_chained` |
| R5 | 游标单调不倒退（按 §4.3 S5 的分轨规则比较） | `replay_revision_not_chained` |
| R6 | manifest：页号覆盖 `1..pages`（**有重复即拒绝**，见 §4.5）、每页 `generation`/`buildId`/scope 与 manifest 逐字段相符 | `replay_manifest_invalid` |
| R7 | `pages === 0` 仅当 §4.3 S4 的固定空构建编码；非空范围须 `pages ≥ 1` | `replay_range_inconsistent` |
| R8 | 覆盖判据按 §4.2 的扫描窗口衔接，**不要求实际消费范围数值连续** | `replay_range_inconsistent` |
| R9 | 修 `builds.js:281`：页包 `lastEventSeq` 取**实际消费**的最后一条（`events[consumedCount-1].eventSeq`），并写 `consumedCount` | 实现修复 |
| S1–S5 | §4.3 形状校验 | 见表 |
| R10 | 哈希校验保留；任一失败即 `throw` 稳定 code，不返回部分结果 | — |

R9 是 F3 唯一的实现侧修复，与 `4f69982` 分属不同位置（后者校验消费游标上界，前者只让页包记录的范围取自实际消费）。

### 4.5 测试矩阵（修订）

全部用例在 **SQLite 模拟器 + Miniflare/workerd D1** 双绑定执行。

**负样本（必须稳定拒绝，且不得返回部分结果）**

| 用例 | 期望 |
|---|---|
| delta 完全缺 scope / 缺 userId / 缺 namespace / 缺 projectionName | `replay_scope_missing` |
| `baseRevision=0, revision=99`（跳号） | `replay_revision_not_chained` |
| 相邻 delta revision 倒退 | `replay_revision_not_chained` |
| revision 非整数 / 非数字 / 负数 / 非安全整数 | `replay_artifact_invalid` |
| `changes`/`rowChanges` 非数组 | `replay_artifact_invalid` |
| `rowChanges` 项缺 `rowKind` 或 `rowKey` | `replay_artifact_invalid` |
| `pages=0` 但 `firstEventSeq=1, lastEventSeq=1`（非空范围零页包） | `replay_range_inconsistent` |
| 空构建用非固定编码（如 `lastEventSeq=null`） | `replay_range_inconsistent` |
| 页包 `lastEventSeq` 与 manifest 区间不符（范围失真） | `replay_range_inconsistent` |
| **页号重复**（`pages=2`，两页都标 page=1） | **拒绝** `replay_missing_page` |
| 缺页（`pages=3` 只给 1、3） | `replay_missing_page` |
| 混入他 scope 的页包 | `replay_scope_mismatch` |
| 页 `generation` 与 manifest 不符 | `replay_manifest_invalid` |
| 页 `buildId` 与 manifest 不符 | `replay_manifest_invalid` |
| 哈希不匹配 | `replay_hash_mismatch`（已有） |

**正样本（必须重放成功且与在线投影逐字节一致）**

| 用例 | 说明 |
|---|---|
| 合法零事件构建：`firstEventSeq=null, lastEventSeq=0, pages=0` | 固定编码 |
| **输入顺序打乱但排序后完整** | **应通过** —— 重放按页号/版本排序，不依赖输入文件顺序 |
| **部分消费**：reducer 消费数 < 页长 | 验证 R9：页包范围取实际消费，重放仍完整 |
| **多用户序号空洞**：A 占 1,3,5；B 占 2,4,6；A 两页消费 `[1,3]`、`[5,7]` 区间含 B 的序号 | 验证 §4.2：实际消费范围不连续仍合法 |
| 跨页回访已有键（同一 topic/problem 在多页出现） | 累计正确（亦为 F1 回归面） |
| 页内重复键（同一 topic 在一页内多次出现） | 累计正确，胜者为页内最后胜出者（F1 §2.4 回归面） |
| 多页构建（≥ 3 页）完整重放 | 基线 |
| 包 scope 字段与 delta 相同但**键顺序不同** | 应通过（R2 不依赖键序） |
| **跨用户同键隔离**：B 用户有同名 topic/problem 且值不同 | A 的构建读回的必须是 A 的值，不得读到 B 的 |
| **不同 generation 同键隔离**：旧 generation 与当前 `stagingGeneration` 有同键 | 读回的必须是 `stagingGeneration` 的值，不得读到旧代 |

---

## 5. 完整预算账目（**修订新增**）

依据 `io/budget.js` 与 `io/invocation-io.js` 的实测语义：

- 类别：`d1` / `queue` / `http`；`prepare()` 不计数，`first()`/`all()`/`run()` 各记 1 次 `d1`，`batch()` 记 **1 次** `d1`（不按语句数）；`queue.send` 记 1，`sendBatch` 记 n；`fetch` 记 1。
- **失败的调用同样计数**（先 consume 后发出）。
- 配额：write 20、query 12、**projection 24**、archive 16、recovery 32、dlq 8；`BUSINESS_SUBREQUEST_CAP = 40`，`PRODUCT_HARD_CAP = 50`。

### 5.1 单个构建页 invocation 账目（投影配额 24）

| # | 步骤 | 类别 | 次数 |
|---|---|---|---|
| 1 | `claimForProcessing`（`.all()`） | d1 | 1 |
| 2 | `getTask`（`.first()`） | d1 | 1 |
| 3 | 构建行读取（`.first()`） | d1 | 1 |
| 4 | `loadProjectionHead`（`.first()`） | d1 | 1 |
| 5 | `MIN(event_seq)`（仅 continuation 为空时） | d1 | 0–1 |
| 6 | 本页事件读取（`.all()`） | d1 | 1 |
| 7 | `readStaged`（新增，**引擎强制 ≤ 4**） | d1 | 0–4 |
| 8 | 提交 `batch` | d1 | 1 |
| | **业务阶段小计** | | **6–11** |

### 5.2 收尾账目

| 收尾 | 语句 | 次数 |
|---|---|---|
| `completeTask` | UPDATE `.run()` | 1 |
| `deferTask` | UPDATE `.run()` | 1 |
| `parkNeedsAttention` | UPDATE `.run()` | 1 |
| **`failTask`** | `getTask` `.first()` + UPDATE `.run()` | **2** |
| `verifyBuildActivated` | 2 × `.first()` | 2 |

最坏收尾 = `failTask` 2 + `verifyBuildActivated` 2 = **4**。

### 5.3 预留与总量

- **收尾预留 4 次**（覆盖最坏收尾路径 `failTask` 2 + `verifyBuildActivated` 2）。

- **「检查还剩 4 次」不等于「保留 4 次」**（原稿 `assertBudgetReserve(io, 4)` 只在入口查一次不成立：
  查完之后业务阶段仍可能把那 4 次花掉）。改为**每个业务阶段开始前的闸门**：

  ```text
  剩余预算 ≥ 下一阶段最坏成本 + 收尾预留 4
  ```

  不足则**不进入该阶段**，直接转入收尾（A 类 `budget_reserve_insufficient`）。
  各阶段最坏成本（依 §5.1）：领取与装载 4、首页 `MIN` 查询 1、页读取 1、
  `readStaged` 4（引擎强制）、提交 1。

- **始终使用同一个预算器，不重置**（`createInvocationIo` 每个 invocation 一个 `createBudget`；
  禁止为绕过闸门新建预算器）。

- **11 + 4 = 15 是正常构建页的设计估算，不是完整入口的实测证明。**
  分支路径、失败收尾路径与入口接线仍需**实际 trace 验证**（读 `budget.snapshot().entries` 逐项对账），
  并在实现阶段把实测账目回填本节。若实测与估算不符，停下报告，**不调整配额迁就代码**。

#### 5.3.1 实测账目（已回填，见 `test/rds2-projection-engine.test.js`「measured build-page budget account」）

| 路径 | 实测（`budget.snapshot()`，双绑定一致） | 构成 |
|---|---:|---|
| 激活页（3 事件，staged-reads 2 次） | **9** | load 4 + firstPage 1 + pageRead 1 + **stagedReads 2** + commit 1 |
| 同形状页 + commit 失败（C 类收尾） | **11** | 同上 9（**失败的 commit 仍计 1**，先记账后发出）+ `failTask` 2 |
| `failTask` 单独隔离测量 | **2** | `getTask` 1 + 条件 UPDATE 1（两绑定一致） |

- 实测激活页 9 落在设计带 7–11 内；最坏路径 9 + `failTask` 2 + `verifyBuildActivated` 2 = **13 ≤ 24**。
- **「失败的 commit 仍占一次子请求」已钉进测试**（`wrapD1.batch` 先 `consume` 再发出）——
  这条语义起初被我的测试替身破坏（替身直接 throw，没先记账），导致对账差 1；修正替身后对齐。
- 阶段闸门的实测由既有用例钉住：limit 8 在 firstPage 闸停车、limit 9 恰好多走一条语句
  （`usedLooser === usedTight + 1`），两次都是 A 类暂缓、`failure_count` 不增。

#### 5.3.2 入口接线核查结果（**如实上报的缺口**）

按裁定「入口接线仍需实际 trace 验证」核查：`src/` 中除 `io/invocation-io.js` 的定义外，
**没有任何生产代码调用 `createInvocationIo`**；`src/index.js` 只有 `fetch` + `scheduled`，
`wrangler.toml` 没有队列消费者配置。也就是说：

- dispatcher 会把 `projection_build` / `projection` / `archive_delta` 任务发送到
  `RDS2_PROJECTION_QUEUE` / `RDS2_ARCHIVE_QUEUE`；
- 但**没有消费者**接收这些消息、创建 `limit: 24/16` 的预算化 io 并调用
  `continueBuild` / `projectOne` / `archiveOne`。

因此「总调用 ≤ 投影配额 24」目前只在**测试构造的 io**上被强制，生产入口尚未存在。
这不属于 F1–F3 的授权范围，本轮**不实现**，如实上报：入口消费者（含各入口配额、
预算化 io 装配、`budget_reserve_insufficient` 的重排）**待原定 T10 实施**
（2026-09-07 实现复审裁定：队列接线归原定 T10，不单独立项）。

- 验收：构造「预算刚好只剩收尾所需」的用例，断言真实故障仍能正确计数与退避 ——
  否则故障时会静默退化回 F2 现状。

---

## 6. 实施顺序（审核通过后）

| 步 | 内容 | 理由 |
|---|---|---|
| 1 | **F2**：双绑定探测异常形状 → 建统一转换层 → 接入 `failTask`（C 类）/ park（D 类）/ `rowsWritten` 检查 / `completeTask` 成功清零 / 预算预留检查 | F1 的确定性超限需要 D 类收尾才有稳定归宿；先建分类，后续错误才有正确出口 |
| 2 | **F1**：`planPageReads` + `readStagedWithinBudget`（引擎强制 ≤4）+ continuation 瘦身 + 事务前字节校验 + 页内 Map 规则 | 依赖步 1 的 D 类收尾与预算预留 |
| 3 | **F3**：`archiver.js` 校验 R1–R10 / S1–S5 + `builds.js:281` 的 R9 + 范围契约（§4.2） | 与 F1 的续页范围语义相关，放在 F1 之后便于对账 |
| 4 | 逐项补真实失败回归 → 定点实现 → 双绑定验收 → Node 22 / Node 26 全量回归 | — |
| 5 | 提交逐项 SHA、运行时、失败/通过证据与预算账目；修正文档中「全部可达要求已覆盖」的过早结论 | 审核执行顺序第 3、4 条 |

每步保持「失败样本 → 红灯 → 修订 → 双绑定绿 → SHA」链路；既有正确行为的补测允许首次即绿，但须做变异自查。

---

## 7. 本轮未做 / 授权边界

- 未修改任何源码、测试、迁移；未提交、未 push、未部署。
- 未创建 Queue、未恢复定时任务、未操作远程数据或资源。
- 未清理主仓库 Rev4 遗留；未回退 `4f69982`；未改动 R5 末页就地激活语义，未重新引入空尾页。
- 未进入 T09/T10。
- 业务子请求消耗：**0**。
- 按裁定：不再为文档自身的提交反复修改历史提交表。

---

## 8. 开放项

四项原开放项已由裁定关闭（§0.1），预留值 4 次与块长 32 已获认可（§5.3、§2.3），当前无未决开放项。

实施过程中若出现下列情况，**停下报告，不放宽断言**：

- 真实异常形状与 §3.1 的假设不符；
- 实测预算账目与 §5 的估算不符；
- 为让某个用例通过而需要放宽 §4.2 / §4.3 的范围或形状校验。

---

## 9. 执行记录（随实施追加，不改写上文）

### 9.1 F2（已完成）

| 项 | SHA | 内容 |
|---|---|---|
| 更正合并结论 | `395c609` | 撤回「整个守卫标记集归 B 类」，记入隔离后 1:1 标记映射与按标记分类（§3.1.2–3.1.4） |
| 转换层 | `03926aa` | `errors/storage-error.js`：标记提取 + 分类，双绑定形状钉进 `test/rds2-storage-error.test.js` |
| 收尾分类 | `ddf89c1` | 构建存储故障按类收尾，不再一律 defer |
| 成功清零 + engine 分类 | `aec7316` | `completeTask` 成功事务内清零；engine 提交故障同走分类 |
| 阶段闸门 | `3b9c82b` | 每个构建阶段按「阶段成本 + 收尾预留 4」设闸（`canAffordStage`） |

### 9.2 F1（已完成）

| 项 | SHA | 内容 |
|---|---|---|
| staging 读取 + continuation 瘦身 | `d36f151` | `projection/staging-read.js`（发前测算、≤4 次、四段绑定）+ `planPageReads` + 页内 Map + continuation 只剩定长标量与单个 summary |

### 9.3 F3（已实现，回归全绿）

| 项 | 内容 |
|---|---|
| 校验模块 | `archive/replay-validate.js`：R1 scope 必填、R2 逐字段比较、R4 `revision = baseRevision + 1`、R6 页号 1..pages 无重复、R7/R8 范围与窗口铺贴、S1–S5 形状、`consumedCount = 0` 显式 `replay_range_inconsistent` |
| 写侧 R9 | `builds.js`：页包 `firstEventSeq`/`lastEventSeq` 改为**实际消费范围**，新增 `consumedCount`、`scanFirstCursor`、`scanCursorAfter`；消费必须是读取数组的**非空前缀**，引擎从前缀派生三个量；空页（零事件构建）不产页包 |
| 测试 | `test/rds2-archive.test.js` 新增 9 组 F3 用例（负样本精确码 / 正样本含空构建、键序不同 scope、窗口内跨用户空洞、部分消费）；既有手冻结夹具补扫描游标，generation/buildId 不符改判 `replay_manifest_invalid` |

**F3 实施中的两次自我纠正（如实记录）**：

1. 首跑 9 例中 3 例失败。逐条诊断后**全部是测试构造错误，不是实现问题**：rewind 用例的
   mutation 没有真正回退（`eventSeq: 1` 对上前值 `1`，`1 < 1` 为假）；两个窗口用例的链里
   **漏了 base delta**，导致只有一个 rev=2/base=1 的 delta，链检查先抛 `replay_missing_page`。
   用一次性诊断脚本打印真实 code 与抛点后修正构造，未放宽任何断言。
2. `consumedCount: 0` 起初落在安全整数分支（`replay_artifact_invalid`）；按裁定语义
   「消费结果必须是非空前缀」，改为显式 `replay_range_inconsistent`，整数类型错误仍归
   `replay_artifact_invalid`。

探针 `.rds2-probe-f2-shape.mjs` 已删除：其全部断言（标记提取、CHECK 模式、类别映射、
无泄漏字段）已钉进 `test/rds2-storage-error.test.js`。

### 9.4 变异自查（三轮，14 项变异 13 项红）

按惯例对本轮新增的「首次即绿」用例做变异自查；每项变异只动**一个条件**，跑完即还原：

| # | 被变异的属性 | 变异方式 | 结果 |
|---|---|---|---|
| 1 | staging 读取 ≤4 次硬上限 | `cost > maxCalls` 改恒假 | 红 |
| 2 | 读取计划先去重再计价 | 去重改为原样透传 | 红 |
| 3 | 读取须给收尾预留让位 | 预留检查改恒假 | 红 |
| 4 | 重放 scope 必填 | `requireScope` 开头直接返回 | 红 |
| 5 | `revision = baseRevision + 1` | 链检查改恒假 | 红 |
| 6 | 重放游标不倒退 | 倒退检查改恒假 | 红 |
| 7 | 扫描窗口铺贴 | `validateWindowCoverage` 调用移除 | 红 |
| 8 | 包范围与窗口**相等** | 相等改大于 | 红 |
| 9 | 页号铺贴（无重复且 1..n） | **去重与顺序两道守卫同撤**（同一属性） | 红（夹具修正后） |
| 10 | 租约/CAS 冲突不计真实故障 | B 分支改判 C | 红 |
| 11 | 成功清零——构建激活内联 UPDATE | 删除 `failure_count = 0` | 红 |
| 12 | 成功清零——`completeTask` | 删除 `failure_count = 0` | 红（**补测后**，见下） |
| 13 | 预算闸门须含收尾预留 | `+ reserve` 移除 | 红 |
| 14 | —— | （第 9 项初版夹具被计数检查 masking，重做后并入第 9 项） | — |

**审计抓出的两个真问题（都已修）**：

1. **「页号重复」用例初版是空洞的**：夹具里包数 ≠ `pages`，计数检查先于去重检查触发，
   重复检测永远轮不到。修正夹具为「包数 = pages = 3、其中两包同页号」，使计数检查无法 masking；
   由于重复页号必然同时破坏 1..n 顺序，变异按**属性级**同时撤掉两道守卫（记为一项）。
2. **「成功清零」存在两份实现**：构建激活的内联 UPDATE 与 `completeTask`（settled 路径）。
   审计变异打在 `completeTask` 上时用例仍绿——不是测试空洞，而是**那份实现没有用例钉住**。
   已补 `F2 completeTask clears the counter on the settled-build path too`，随后两条清零路径
   各自变异转红（第 11、12 项）。

另外补上 `readStagedWithinBudget` / `planReadCost` 的直接单测
（`test/rds2-staging-read.test.js`，5 例）：发前计价、超上限一次都不发、去重计价、
预留不足即等待、四段 scope+generation 绑定隔离。此前该接口只被小计划间接经过，上限从未被触发。

审计后全量回归（`72abfd2`）：**Node 22.22.2 与 Node 26.7.0 均 worker 596/596、bridge 47/47 全绿**
（本轮自 552 起累计 +44）。

### 9.5 实现复审后的三项 P1 修复（2026-09-07 复审裁定，随记录追加）

2026-09-07 实现复审（审核 HEAD `45ff5f1`）裁定：G2 暂不通过，保留已完成修复，三项 P1 落差
在既有 G2 授权内补齐（可补测试、定点修复、独立提交；改契约/配额先报告）。每项均提供
真实失败断言、双绑定结果、服务预算 trace 与运行时回归；三项独立提交：

| 项 | SHA | 修复内容 |
|---|---|---|
| F3-R1 | `8e7fb8b` | 跨字段重放契约钉死：激活 `eventSeq === (pages===0 ? 0 : manifest.lastEventSeq)`；包 `scanFirstCursor <= firstEventSeq <= lastEventSeq <= scanCursorAfter - 1`（且首尾恒等）；`consumedCount <= lastEventSeq - firstEventSeq + 1`、count=1 → 首尾相同；首页 `firstEventSeq === manifest.firstEventSeq`；普通增量严格递增（`<=` 拒）而激活可重述目标（仅 `<` 拒）。`test/rds2-archive.test.js` +6（五个审核反例 + 重述正例），40/40 |
| F2-R1 | `1b481c9` | ① 边界覆盖取租约后全生命周期：`continueBuild` / `projectOne` 双入口 `runLeased` 包裹（claim 留在边界外——无租约不伪造收尾；`verifyAuthoritative` 按条件传，build 未加载传 null 保 B 类诚实）；② 收尾自身失败归稳定 E 码（`closeout_verify_unavailable` / `closeout_park_unavailable` / `closeout_fail_unavailable` / `budget_exhausted_no_closeout`），恢复路径 = 租约过期 + reclaimStale，不为收尾重试；③ `INTERNAL_DETERMINISTIC_CODES` 白名单优先于文本提取（9 个内部 code → D；`budget_exhausted` → A；`ERR_SQLITE_ERROR` 等驱动 code 不误入白名单）。附脱敏结构化日志：成功转换后恰一行，经新 `io.log` sink（非预算项），闭集字段 `event/taskId/outcome/class/code/at`。测试：`test/rds2-close-out.test.js` 新套件 7 例、storage-error +3（21/21）、引擎三阶段读故障注入 + 增量 head 故障 + 三处语义改写 |
| F1-R1 | `7171ed3` | summary 唯一权威表示：reducer continuation 只剩 `{nextEventSeq, stagedCount, page}`（builds 附 `firstEventSeq` / `summary`），accumulators 从 `continuation.summary` 恢复（旧顶层字段保留为读侧 fallback）；长 topic 在 continuation_json 从 3 次降为 2 次（`currentTopic` + `latest.topic`）。事务前字节校验：`MAX_BUILD_STATE_BYTES = 65536`（与存储 CHECK 同口径）对 savedContinuation 与激活 summary 做 canonical + UTF-8 测量，超限抛确定性 `build_state_too_large`（D 类：事务前拒绝、零持久化、failure_count 保持 0）。测试：23k×2 pageSize=1 完成、continuation 闭集键 + 长字段只在 summary + ≤65536、33k 单条事务前拒绝；continuation 键白名单收紧为闭集 |

**确定性行为变化（复审确认的语义，三处旧测试按此改写）**：`changes_too_large` /
`commit_batch_too_large` / `invalid_page_size` 不再外抛穿越服务边界，改为 D 类停车——
「不截断、无半写」属性不变，`failure_count` 保持 0（确定性错误不计数）。

**33k 用例的构造约束（记录，避免后人踩坑）**：长 topic 经 `algorithmReducer` 会进入
rowChanges 约 10 处（rowKey / sortKey / memberKey / topic_problem rowKey 等），build package
先撞 256KiB 的 `changes_too_large`，永远到不了 64KiB 的 build-state 校验——
「10L < 256KiB 且 2L > 64KiB」无解。故 33k 用例用自定义 `longSummaryReducer`
（summary 携带长 topic ×2、rowChanges 极小）隔离字节校验路径。

**变异自查（本轮新增断言，5 项变异全部红，跑完即还原）**：

| # | 被变异的属性 | 变异方式 | 结果 |
|---|---|---|---|
| 1 | continuation 键白名单（收紧后闭集） | `algorithm.js` 在 continuation 顶层复活 `counts` | 红（`unexpected continuation field: counts`） |
| 2 | build-state 事务前字节校验 | `assertBuildStateBounded` 超限判断改恒假 | 红（33k 用例 code 退化为 driver 级 `storage_check_violation`，且不再是事务前拒绝语义） |
| 3 | 内部确定性 code 优先于文本提取 | `INTERNAL_DETERMINISTIC_CODES` 命中检查改恒假 | 红（诱饵 `stale_task_write` 文本劫持分类为 B） |
| 4 | 收尾 park 自身失败归 E | 移除 park 的 try/catch | 红（raw 错误穿透 `closeOutFailure`） |
| 5 | 脱敏日志闭集（**属性级，两道防线同撤**） | 调用方注入 `message` + `logLine` 改透传 spread | 红（字段闭集 `deepEqual` 破 + 泄漏断言破） |

第 5 项的如实记录：**单撤调用方一层不红**——`logLine` 的白名单解构会丢弃多余字段，
日志闭集由「调用方闭集传参」+「序列化白名单解构」两层共同保证，测试钉住的是组合属性
（同 §9.4 第 9 项「两道守卫同撤记一项」的先例）。还原后基线确认：storage-error +
close-out 全量 28/28 绿；引擎侧基线由最终提交态全量（242/242）覆盖（工作树 == HEAD）。

**运行时回归**：F2 提交前全量（纯 F2 工作树，双绑定一致）**239/239**；F1 提交前全量
（F1 恢复态）**242/242**；最终提交态（`7171ed3`，含收紧后的 continuation 键白名单）
全量 **242/242**，双绑定一致、0 失败。

### 9.6 实现复审（第二轮）的两项 P1 修复（2026-09-07 复审裁定，随记录追加）

2026-09-07 第二轮实现复审裁定：G2 仍暂不放行，剩两项 P1（复审者在受限环境无法独立
spawn Node 测试，不作为证据；两项缺陷均由代码差异/纯函数复现直接确认）。按授权
「每项红灯 → 实现 → 双绑定验证 → 独立 commit」执行，两项独立提交：

| 项 | SHA | 修复内容 |
|---|---|---|
| P1-2 | `fffd3fa` | `completeArchive` 的 guarded UPDATE（archiver.js）置 completed 但遗漏清零——与 `completeTask` 契约（repository.js:98，注释明言 success clears the counter）直接不一致。在同一原子 batch 的 UPDATE 中补 `failure_count = 0`。回归：首次 readback 503（真实失败 → `failTask` 计数 1、pending、退避 30s）→ +31s 重新 claim → 精确 find 复用已上传对象（不重传）→ 成功归档 → 断言 completed 且 failure_count=0。红灯即该变异本身（移除清零 = HEAD 旧态）：`(sqlite) 1 !== 0` |
| P1-1 | `5d74784` | `readStagedWithinBudget` 同类合并去重：新增 `normalizeReadPlan` 按 `rowKind` 聚合 `Set<rowKey>`，校验（kind/key）、合并、去重、计价与上限判断全部在任何 D1 查询前完成；`planReadCost` 与真实查询**共用同一归一化结果**。复审复现场景（两条 topic read 含相同 key + 一条 problem read）成本/查询 = 2 而非 3；超上限仍零查询（例 2 在合并语义下依旧钉住） |

**P1-1 的既有用例调整（语义保持，非放水）**：原「超上限」用例为 3 条 topic read × 33 keys——
分散计价下 = 10 chunks，但合并计价下 99 个 unique keys 只有 4 chunks，不再超上限。
调整为 **5 条 × 33 keys**（165 个 unique keys，合并计价 = 6 chunks > 4）：旧实现的分散计价
给 5×`ceil(33/32)`=**10**，因此该用例在红灯阶段就直接暴露了分散计价缺陷（期望 6，旧实现
给 10）；实现后按合并语义转绿，`build_read_limit_exceeded` + 零查询断言原样保留。

**变异自查（4 项，全部红，跑完即还原）**：

| # | 被变异的属性 | 变异方式 | 结果 |
|---|---|---|---|
| 1 | 计价复用归一化结果 | `planReadCost` 撤出 `normalizeReadPlan`、恢复旧逐条计价（查询侧保持归一化） | 红（超上限用例 10≠6、复审场景 3≠2） |
| 2 | 查询复用同一归一化结果 | 查询循环撤出归一化 reads、恢复逐条原始声明执行（计价保持归一化） | 红（复审场景预算消耗 3≠2） |
| 3 | rowKind 白名单（重构后守卫迁移仍被钉住） | `normalizeReadPlan` 删除 kind 校验 | 红（`build_read_kind_rejected` 断言） |
| 4 | key 形状校验（同上） | `normalizeReadPlan` 删除 key 校验 | 红（`build_read_key_invalid` 断言） |

第 3、4 项为补课：实现把校验从旧 `planReadCost` 搬进 `normalizeReadPlan`（守卫位置变化），
6/6 绿只证明行为保持，变异才证明测试对**新位置**的守卫仍有抓力。两项均为单条件变异，
还原后定向 6/6 绿、工作树与 HEAD 一致。

执行事故如实记录：变异 1 还原时误用 `git restore`，把**尚未提交的实现**一并还原到 HEAD
（实现丢失、测试回红）——重读了文件并重放实现。教训重申：`git restore` 只适用于还原
**已提交基线**的改动；未提交实现还原只能用 Edit 反向恢复。

**projection-engine 全文件的偶发失败（环境级，非本轮代码回归）**：受影响面验证中三次
全文件跑结果为 ①与 archive 全文件并行 57/58（红 `F1 200-event multi-topic`）→ ②独占
57/58（红 `R2 50-event default page size`）→ ③独占 **58/58 绿**。两次失败用例互不相同、
单跑均绿（181s / 101s，同代码状态）、第三次全绿——排除确定性回归与顺序依赖，判定为
长时间高负载下 Miniflare/workerd 偶发故障。全量回归（见下）再证 244/244。

**运行时回归（最终提交态 `5d74784`，双绑定）**：全量
`node --test test/rds2-*.test.js test/end-to-end.test.js` → **244/244，0 失败**
（242 + 本轮 staging-read 与 archive 各 +1）。双运行时核对：Node 22.22.2 与
Node 26.7.0 均 **244/244**（Node 26 的 `--test` 为 spec reporter，汇总行是
`ℹ pass 244 / ℹ fail 0` 而非 TAP 的 `#` 前缀——过滤 grep 要按格式区分）。

### 9.7 实现复审（第三轮）的 P1-3：计划形状与每页键数约束（2026-09-07，随记录追加）

第三轮复审确认前两项 P1 已修复（定向 47/47 独立通过），裁定剩一项 P1：批准方案 §2.2
的「每个 rowKind 唯一键数 ≤ 本页事件数、且 ≤ 50」未实现——实测 128 个 topic 键以
cost=4 被接受进入查询；且 `rowKeys: "abc"` 在 `keys.some` 处抛**无稳定 code 的裸
TypeError**，可能被误分类为 C 类临时故障反复重试；另发现 `rowKeys: null` 被
`?? []` 静默吞成空读取。复审确认 pageSize ≤ 50 已由 `MAX_BUILD_PAGE_SIZE = 50`
（builds.js）保证，缺口只在 staging-read 层。

**修复（`2e83691`，4 文件 +199/-42）**，三处：

1. `staging-read.js`：`readStagedWithinBudget` / `planReadCost` 接收 `eventCount`
   （builds.js 传 `events.length`）；`normalizeReadPlan(plan, eventCount)` 在任何
   D1 查询前完成：eventCount 必须是安全非负整数、`plan.reads` 必须是数组
   （新 code `build_read_plan_invalid`，已加入内部确定性白名单 → D 类）、rowKeys
   必须是**非空字符串数组**（`build_read_key_invalid`）、合并去重后每 rowKind
   唯一键数 ≤ min(eventCount, `STAGING_READ_MAX_KEYS_PER_KIND = 50`)，超限抛
   `build_read_limit_exceeded`（detail 带 rowKind/keys/eventCount/maxKeysPerKind）
   → 现有 D 类路径停车。计价与查询继续共用同一归一化结果；全部拒绝发生第一次
   查询之前（budget.used 保持 0）。
2. `builds.js`：唯一调用点传 `eventCount: events.length`。
3. `storage-error.js`：`INTERNAL_DETERMINISTIC_CODES` 白名单加
   `build_read_plan_invalid`（10 个；无闭集数量断言，安全扩容）。

**测试（staging-read 6 → 10 例，双绑定）**：既有 6 例全部参数化 `eventCount`
（例 2 改判键数上限语义：165 unique keys > 50，`detail.maxKeysPerKind` 钉住触发原因；
另断言 11 keys/eventCount=10 钉住 eventCount 维度——两道上限独立）。新增 4 例
（复审红灯清单）：rowKeys 为字符串/对象/null 稳定拒绝不抛裸 TypeError；
51 unique keys on a 50-event page 零查询拒绝；跨两条同类声明合计 51 同样拒绝
（上限判的是合并后的唯一键数）；重复声明 5 次但唯一 10 ≤ eventCount=10 仍合并
执行且 cost=1。红灯：4 红（新行为）+ 6 绿（保持类）+ T4 旧实现即绿（合并语义
已由 P1-1 满足，属防误伤守卫）。

**变异自查（3 项，全部红，Edit 反向还原后 10/10 绿）**：

| # | 被变异的属性 | 变异方式 | 结果 |
|---|---|---|---|
| 1 | 每页键数上限 | `keys.size > eventCount \|\| keys.size > 50` 改恒假 | 红（例 2 + 51/50 + 分散 51 三处） |
| 2 | rowKeys 数组形状校验 | 恢复 `?? []` + 直接 `.some` | 红（裸 TypeError 无 code / null 静默跳过，T1 抓到） |
| 3 | eventCount 契约校验 | 删除 `Number.isSafeInteger` 检查 | 红（T1 的 eventCount 断言） |
| 4 | 新 code 的 D 类分类钉住 | `INTERNAL_DETERMINISTIC_CODES` 删除 `build_read_plan_invalid` | 红（§3.2 分类测试的 code 数组补入该 code 后，误删即红——防退化到 C 类盲目重试） |
| 5 | builds.js 的 eventCount 接线 | 调用点删 `eventCount: events.length` | 红（对账用例，错误输出顺带端到端证实 `build_read_plan_invalid` → class D → needs_attention 停车） |

第 4 项为分类测试补强（`F2-R1 internal deterministic codes are class D` 的 code 数组
原为旧 9 个闭集，新 code 入白名单时未同步——误删条目时该测试不红，分类静默退化为
C 类兜底）。第 5 项的变异输出本身就是要求 5「由现有 D 类路径停车」的端到端证据：
`(sqlite) {"outcome":"needs_attention","code":"build_read_plan_invalid","class":"D"}`。

**运行时回归（最终提交态 `2e83691`，双绑定）**：复审指定受影响面
`node --test --test-concurrency=1 test/rds2-staging-read.test.js test/rds2-projection-engine.test.js`
→ **68/68**（staging-read 10 + 引擎 58；builds.js 接线由引擎页全覆盖验证）。
全量双 Node：Node 22.22.2 **248/248**；Node 26.7.0 首跑 247/248——失败的是
`F1 200-event multi-topic`（§9.6 已记录的 flaky 对象），错误发生在 **seed 阶段**
（`seedEventsThroughAccept` → `request_id_conflict` 409，accept 层，P1-3 未触碰），
且耗时仅 428ms（正常单跑 ~184s）——快速失败是偶发故障的强信号；单跑复测绿
（184s 完整执行），Node 26 独占复跑全量 **248/248**。判定：环境偶发（workerd 长跑
偶发，seed 阶段冲突是其又一表现形态），非代码回归。

**执行事故如实记录**：红灯测试首版 import 了未导出的 `STAGING_READ_MAX_KEYS_PER_KIND`，
ESM 模块实例化直接失败（文件级 not ok，用例全跑不到）——先补常量导出（无行为）让红灯
以用例级呈现；实现首版 `normalizeReadPlan` 输入 `reads` 与输出 `reads` 同名重复声明
（SyntaxError），输出改名 `normalized` 修复。

### 9.8 终审 P1-4：`plan.reads` 的严格数组契约（2026-09-07，随记录追加）

终审确认 P1-1 / P1-2 / 键数双上限 / 分类闭集 / `builds.js` 接线均符合预期，裁定剩最后
一项 P1：`normalizeReadPlan` 用 `plan?.reads ?? []` 兜底，使 `null`、`{}`、
`{ reads: null }`、`{ reads: undefined }` 被静默当成**合法空计划**（cost 0），
而冻结文档要求「`plan.reads` 必须是数组，否则 `build_read_plan_invalid`」。既有用例
只覆盖 `{ reads: 123 }`，因此测试全绿但契约未落实。

**修复（独立提交）**：`normalizeReadPlan` 严格要求
① `plan` 是非 null 对象；② `reads` 是**自有字段**（`Object.prototype.hasOwnProperty`，
排除继承字段）；③ `plan.reads` 是数组。三者任一不满足 → `build_read_plan_invalid`。
**唯一合法的空计划是 `{ reads: [] }`**。计价与执行走同一严格校验（终审要求 5），
拒绝全部先于首次查询（`budget.used === 0`）；`build_read_plan_invalid` 仍归 D 类
→ `needs_attention`，分类闭集未调整（终审要求 7）。

**测试（staging-read 10 → 11）**：新增 `P1-4 the only legal empty plan is { reads: [] }`
——七种非法输入（null / undefined / {} / reads:null / reads:undefined / reads:123 /
reads:"abc"）各在 `planReadCost` 与 `readStagedWithinBudget` 两处断言
`build_read_plan_invalid`，并断言显式空计划仍 cost 0、零查询。红灯：新用例红，
其余 10 例全绿。

**变异 6（终审点名要求）**：把严格校验恢复成 `plan?.reads ?? []` → 新用例转红 ✓。

**变异 7（补课：自有字段守卫的独立抓力）**：严格契约有三道守卫（plan 非 null 对象 /
reads 是自有字段 / reads 是数组）。只撤掉 `hasOwnProperty` 时，`{}` 仍会被
`Array.isArray(undefined)` 拦住，测试全绿——即该守卫当时**没有独立抓力**。唯一能区分
「字段存在」与「字段自有」的输入是**继承字段**（`Object.create({ reads: ["inherited"] })`
读得到且是数组，却不自有），补该断言后再撤守卫 → 转红 ✓。这再次印证 §9.4 第 9 项与
§9.6 第 3-4 项的教训：**属性由多层守卫组成时，先造出能让各层独立显形的输入再变异**。

**契约收紧的兼容性核查（静态 + 动态）**：生产的 `algorithmReducer.planPageReads`
返回 `{ reads: [...] }`（合法）；引擎测试内无任何自定义 `planPageReads`；
builds.js 的 fallback 是 `{ reads: [] }`（合法）。故收紧对生产与既有测试零影响，
由 projection-engine 接线测试与双 Node 全量回归验证。

**运行时回归（最终提交态，双绑定）**：定向
`node --test test/rds2-staging-read.test.js test/rds2-storage-error.test.js` → **32/32**
（staging-read 11 + storage-error 21）；projection-engine 接线 **58/58**；
全量双 Node：Node 22.22.2 **249/249**、Node 26.7.0 **249/249**，均 0 失败
（248 + 本轮新增 1 例；本轮未再出现 §9.6 / §9.7 记录的 workerd 偶发）。
