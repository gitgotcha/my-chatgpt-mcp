{
  "name": "reliable-drive-sync",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "npm run test:worker && npm run test:bridge",
    "test:worker": "node --test services/reliable-drive-sync-worker/test/*.js",
    "test:bridge": "node --test tools/reliable-drive-sync-mcp/test/*.mjs",
    "deploy:worker": "npx wrangler deploy --config services/reliable-drive-sync-worker/wrangler.toml"
  }
}
